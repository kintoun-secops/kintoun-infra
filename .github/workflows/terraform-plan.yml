# PR 에서 plan 만 수행 (읽기 전용 롤).
# 사전 조건: bootstrap 출력 plan_role_arn 을 저장소 Variables 의 AWS_PLAN_ROLE_ARN 에 등록.
# 루트 모듈은 backend.tf 와 .github/terraform-roots.json 으로 자동 발견한다 — 이 파일에 디렉터리를 적지 않는다.
# 모든 PR 에서 모든 루트를 plan 한다 (paths 필터 없음: 필터로 건너뛴 required check 는 머지를 막는다).

name: terraform plan

on:
  pull_request:

permissions:
  id-token: write
  contents: read
  pull-requests: write

# 같은 PR 의 이전 커밋 plan 은 취소한다. apply 와는 다른 그룹이다.
concurrency:
  group: plan-${{ github.event.pull_request.number }}
  cancel-in-progress: true

env:
  CONFTEST_VERSION: "0.69.0"

jobs:
  # 자격증명 없이 도는 검사. 모든 루트가 대상이다.
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
      - uses: hashicorp/setup-terraform@dfe3c3f87815947d99a8997f908cb6525fc44e9e # v4.0.1
        with:
          terraform_wrapper: false
      - run: terraform fmt -check -recursive
      - name: 루트 모듈 목록 검증
        run: |
          node .github/scripts/test-tf-roots.js
          node .github/scripts/tf-roots.js
      - name: validate (backend·자격증명 없이)
        run: |
          for d in bootstrap $(node .github/scripts/tf-roots.js --list); do
            echo "::group::$d"
            terraform -chdir="$d" init -backend=false -input=false >/dev/null
            terraform -chdir="$d" validate -no-color
            echo "::endgroup::"
          done
      - uses: terraform-linters/setup-tflint@1cf010d3c7aef302051ccdb68c14c5dc2efa34ef # v6.3.1
        with:
          tflint_version: v0.64.0
      - name: tflint
        env:
          GITHUB_TOKEN: ${{ github.token }} # 플러그인 내려받기 rate limit 회피
        run: |
          tflint --init --config "$GITHUB_WORKSPACE/.tflint.hcl"
          tflint --recursive --config "$GITHUB_WORKSPACE/.tflint.hcl" -f compact
      - name: conftest 설치
        run: |
          curl -sSfL -o /tmp/conftest.tar.gz \
            "https://github.com/open-policy-agent/conftest/releases/download/v${CONFTEST_VERSION}/conftest_${CONFTEST_VERSION}_Linux_x86_64.tar.gz"
          sudo tar -xzf /tmp/conftest.tar.gz -C /usr/local/bin conftest
      - name: 정책 테스트
        run: conftest verify --policy .github/policy
      - name: 정책 파이프라인 통합 테스트
        run: bash .github/scripts/test-iam-pipeline.sh
      - name: Access Analyzer 입력 테스트
        run: node .github/scripts/test-validate-iam-policies.js
      - name: IAM 코멘트 조합 테스트
        run: node .github/scripts/test-iam-comment.js

  # backend.tf 스캔 == 매니페스트 여야 통과한다. 여기서 실패하면 plan 은 돌지 않는다.
  discover:
    runs-on: ubuntu-latest
    outputs:
      roots: ${{ steps.roots.outputs.roots }}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
      - id: roots
        run: node .github/scripts/tf-roots.js --github-output

  plan:
    needs: [lint, discover]
    strategy:
      fail-fast: false
      matrix:
        dir: ${{ fromJSON(needs.discover.outputs.roots) }}
    uses: ./.github/workflows/_tf-root.yml
    with:
      dir: ${{ matrix.dir }}
      mode: plan

  # 모듈마다 코멘트를 달면 PR 이 봇 코멘트로 뒤덮인다. 모아서 한 번만 게시한다.
  iam-comment:
    runs-on: ubuntu-latest
    needs: [discover, plan]
    if: always()
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
      - uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        continue-on-error: true
        with:
          pattern: iam-findings-*
          path: findings
      - uses: actions/github-script@f28e40c7f34bde8b3046d885e986cb6290c5673b # v7.1.0
        id: render
        env:
          TF_ROOTS: ${{ needs.discover.outputs.roots }} # discover 가 실패하면 비어 있다 → 미검사로 표시
        with:
          script: |
            const render = require(`${process.env.GITHUB_WORKSPACE}/.github/scripts/iam-comment.js`);
            await render({
              github, context, core,
              findingsDir: `${process.env.GITHUB_WORKSPACE}/findings`,
              outFile: `${process.env.GITHUB_WORKSPACE}/iam-comment.md`,
              expectedDirs: JSON.parse(process.env.TF_ROOTS || '[]'),
            });
      - uses: marocchino/sticky-pull-request-comment@5770ad5eb8f42dd2c4f34da00c94c5381e49af88 # v3.0.5
        with:
          header: iam-guard
          path: iam-comment.md
          skip_unchanged: true
          only_update: ${{ steps.render.outputs.only_update }}

  # 브랜치 보호의 required check 는 이 잡 하나만 가리킨다 (plan (…) 이름은 루트가 늘면 바뀐다).
  result:
    runs-on: ubuntu-latest
    needs: [lint, discover, plan, iam-comment]
    if: always()
    steps:
      - run: |
          test "${{ needs.lint.result }}" = success
          test "${{ needs.discover.result }}" = success
          test "${{ needs.plan.result }}" = success
